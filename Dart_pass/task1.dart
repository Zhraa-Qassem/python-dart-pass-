main() {
const hour = DateTime.now( ).hour;
print (hour );
}
 /*This constructor isn’t a constant constructor 
Const variables must be a constant value ( time is a variable) */


//maybe try it like this 
/* var hour = DateTime.now( ).hour;
print (hour );
/*
